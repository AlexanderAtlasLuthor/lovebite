import React from 'react';
import ReactDOM from 'react-dom/client';
import LoveBite from './LoveBite.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LoveBite />
  </React.StrictMode>
);
